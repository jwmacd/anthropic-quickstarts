from anthropic import Anthropicimport httpx# Create a custom transport with retry configurationtransport = httpx.HTTPTransport(    retries=2,  # Keep default number of retries    retry_delay=20.0  # Set retry delay to 20 seconds)# Configure the client with custom transport and timeoutclient = Anthropic(    timeout=httpx.Timeout(60.0),  # Set overall timeout to 60 seconds    transport=transport,)
